// Test 5: Struct and enum definitions

struct Point {
    x: int,
    y: int
}

point.x
point.y

enum Color {
    Red,
    Green,
    Blue
}

enum DataTypes {
    Int,
    Float,
    Float64,
    rune,
    Char,
    String
}

fn main() -> int {
    var p: Point
    var c: Color = Red
    
    return 0
}


impl Point {
    fn radius(x: int, y: int) {

    }

    fn distance(x: int, y: int) {

    }
}

Point.raius(x, y)
Point.grow(x, y)
