var grade = 85
var status = "pass"

fn evaluate_grade(score: int) -> int {
    if score >= 90 {
        return 1
    } else if score >= 80 {
        return 2
    } else if score >= 70 {
        return 3
    } else {
        return 0
    }
}

/*
fn get_letter(level: int) {
    match level {
        1 => return "A"
        2 => return "B"
        3 => return "C"
        _ => return "F"
    }
}
*/
fn main() {
    var level = evaluate_grade(grade)
    return level
}